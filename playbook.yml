---
- name: Configure Server
  hosts: servers
  become: yes

  tasks:
    - name: Install Apache Web Server
      apt:
        name: apache2
        state: present

    - name: Start Apache Service
      service:
        name: apache2
        state: started
        enabled: yes
